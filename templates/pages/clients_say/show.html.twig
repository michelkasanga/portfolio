{% extends 'base.html.twig' %}

{% block title %}ClientsSay{% endblock %}

{% block body %}
    <h1>ClientsSay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clients_say.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ clients_say.fullName }}</td>
            </tr>
            <tr>
                <th>Profession</th>
                <td>{{ clients_say.profession }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td> <img src="{{ vich_uploader_asset(clients_say, 'imageFile')}} " ></td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ clients_say.message }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ clients_say.createdAt ? clients_say.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ clients_say.updatedAt ? clients_say.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clients_say_index') }}">back to list</a>

    <a href="{{ path('app_clients_say_edit', {'id': clients_say.id}) }}">edit</a>

    {{ include('pages/clients_say/_delete_form.html.twig') }}
{% endblock %}
